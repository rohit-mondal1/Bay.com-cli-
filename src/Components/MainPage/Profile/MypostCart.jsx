import React from 'react';

const MypostCart = ({kk}) => {
    const {email, imgurl , text} = kk;
    console.log(kk);

    return (
        <div>
            
        </div>
    );
};

export default MypostCart;